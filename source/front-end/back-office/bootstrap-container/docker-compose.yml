version: "3.4"
services:
  nginx-react:
    container_name: ngixreactapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9000:9000
    environment:
      NODE_ENV: production
