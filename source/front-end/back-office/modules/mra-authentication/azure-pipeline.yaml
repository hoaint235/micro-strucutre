name: $(BuildID)

trigger:
  branches:
    include:
      - master
      - feature/*
  paths:
    include:
      - "/source/front-end/back-office/modules/mra-authentication/*"
pr:
  - master

pool:
  vmImage: "ubuntu-latest"

steps:
  - script: |
      echo "Hello from mra-authentication folder."
