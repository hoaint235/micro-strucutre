version: "3.5"

services:
  account-service:
    container_name: account-service
    build:
      context: ../
      dockerfile: ${BE_BASE_FOLDER}/Account/Dockerfile-compose
    environment:
      - ASPNETCORE_ENVIRONMENT=Debug
      - ConnectionStrings__SqlConnection=${BE_ACCOUNT_CONNECTION_STRING}
    ports:
      - "${BE_ACCOUNT_PORT}:80"
    restart: always
    networks:
      - back-end
networks:
  back-end:
